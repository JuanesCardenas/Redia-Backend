spring:
  datasource:
    url: jdbc:mysql://localhost:3306/redia_restaurante?useSSL=false&serverTimezone=UTC
    username: root
    password: 1234

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

server:
  port: 8080

jwt:
  secret: RediaRestauranteClaveUltraSecreta123456
  expiration: 86400000 # 24 horas en milisegundos